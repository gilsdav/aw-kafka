{
  "name": "test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start-docker": "docker-compose up -d",
    "start-front":"http-server",
    "stop-docker": "docker-compose down",
    "start-front-server": "cd FrontendServer && dotnet restore && dotnet run",
    "start-history-server": "cd HistoryServer && dotnet restore && dotnet run"
  },
  "author": "David Gilson <davgilson@live.fr>",
  "license": "ISC",
  "dependencies": {
    "@aspnet/signalr": "^1.1.0"
  },
  "devDependencies": {
    "http-server": "^0.11.1"
  }
}
